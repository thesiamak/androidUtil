package ir.drax.util

import android.app.Activity
import android.content.Context
import ir.drax.modal.Modal

class Try(context: Activity) {
    init {
        Modal.builder(context).apply {

        }.build()
    }
}